apiVersion: v1
kind: Service
metadata:
  name: keycloak-service
  namespace: student-j-donnerbauer
  labels:
    app: keycloak
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
    - name: https
      port: 8443
      protocol: TCP
  type: ClusterIP